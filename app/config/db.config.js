// @HOST, siempre será localhost porque la comunicacion entre la app y la db siempre sera a través de la API
// @PORT, puerto por defecto de MongoDB
// @DB, nombre de la base de datos
module.exports = {
    HOST: "localhost",
    PORT: 27017,
    DB: "turnos_db"
  };